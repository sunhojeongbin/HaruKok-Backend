# 필수 패키지 설치 명령어

# DTO 검증 및 변환을 위한 패키지

npm install class-validator class-transformer

# UUID 생성을 위한 패키지

npm install uuid @types/uuid

# 암호화를 위한 패키지 (향후 사용)

npm install bcrypt @types/bcrypt

# 날짜 처리를 위한 패키지 (권장)

npm install dayjs

# 환경별 설정 파일 로드를 위한 패키지

npm install cross-env

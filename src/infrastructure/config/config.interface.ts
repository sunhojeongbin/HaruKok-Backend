export interface EnvConfig {
  PORT: number;
  NODE_ENV: string;
  SWAGGER_AUTH_TOKEN: string;
  DB_HOST: string;
  DB_PORT: number;
  DB_USER: string;
  DB_PASS: string;
  DB_NAME: string;
  KAFKA_BROKER: string;
}
